services:
  watcher:
    image: openwebui-watcher:local
    build: .
    container_name: watcher
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - /srv/openwebui-inbox:/inbox:ro
    networks:
      - proxy # example network to reach openwebui

networks:
  proxy:
    external: true
