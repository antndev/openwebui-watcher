services:
  watcher:
    image: openwebui-watcher:local
    build: .
    container_name: watcher
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - /srv/openwebui-inbox:/inbox
